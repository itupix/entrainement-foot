name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"  # toutes les 10 minutes
  workflow_dispatch:        # pour pouvoir le déclencher manuellement

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render app
        run: |
          curl -sSf https://entrainement-foot.onrender.com/healthz || true